# Lessons

## User Specified Lessons

- You have a python venv in ./venv (create if needed). Check `which uv` first; if available use `uv pip install`, else `pip`.
- For multiline commit messages, write the message to a file and use `git commit -F <filename>`, then delete the file. Include "[Cursor] " in commit messages and PR titles.

## Cursor learned

- This repo (GigaLearnCPP) was developed on MSVC; GCC needed: Timer clock fix, include-path case fixes, -fPIC on static libs, no std::iterator, cxxabi demangling.
- libtorch 2.x requires CMake >= 3.18; CPU build for Linux: https://download.pytorch.org/libtorch/cpu/libtorch-cxx11-abi-shared-with-deps-2.5.1%2Bcpu.zip -> extract to GigaLearnCPP/libtorch (gitignored).
- On this VM, `c++` is clang++ without libstdc++ dev files: always configure with -DCMAKE_CXX_COMPILER=g++.
- `configure_file(... COPY)` is invalid CMake; must be COPYONLY.
- `torch::tensor(std::vector<T>)` is ~2.7x slower than `from_blob(...).clone()` for big buffers (measured).
- gather()/index_copy_ in libtorch require kInt64 index tensors (runtime error otherwise).
- Test CMake globs need CONFIGURE_DEPENDS or a manual reconfigure to pick up new files.
- tmux sessions get reaped in this environment; run long builds as foreground Shell commands with big block_until_ms instead.
- Collision meshes: synthetic box mesh can be generated for smoke tests (struct: int32 numTris, int32 numVerts, tris as 3x int32, verts as 3x float, little-endian, .cmf in collision_meshes/soccar/).
- rlgym-ppo interop: GigaLearn model files are POLICY.lt/CRITIC.lt/SHARED_HEAD.lt (torch::save archives, loadable with torch.jit.load in Python).

# Scratchpad

## Task: Improve GigaLearnCPP (bugs, speed, docs, setup, roadmap) — branch cursor/improve-gigalearn-e1ff, PR #1

### Status: COMPLETE — all items done, verified, committed, pushed, PR updated

[X] Full codebase audit (learner, PPO, GAE, envset, models, utils, obs/actions/rewards, RLBot, tools, python scripts)
[X] 25+ bugs fixed (see PR body / commit messages for the full list); highlights:
    - GAE truncation-order corruption + OOB bootstrap read
    - Overbatching silently dropping experience
    - EnvSet arena-creation order race misattributing game events
    - Inverted boost pad timers giving orange team blue's timers
    - DefaultAction dead action + 0-boost jump+boost actions
    - Welford JSON key mismatch crashing standardizeObs checkpoint loads
    - trainAgainstOldVersions trajectory gap corruption (roadmap item finished)
    - RLBot pad ordering scrambled in-game obs
    - MSVC-only include resolution + case + -fPIC (Linux support)
[X] Perf: one GPU sync per iteration (was ~8/minibatch), from_blob conversions, vectorizable NaN scan, no hot-loop temporaries, index-tensor batching
[X] Features: normalizeAdvantages, targetKLDiv early stop, gradClipNorm, wandb fallback logger, atomic checkpoint saves, GGL_NATIVE_ARCH, sys.path fix, mesh warning
[X] Tests: 52 unit tests, all passing (GAE regressions, masks, stats, buffers, models, obs symmetry, env components)
[X] CI: GitHub Actions Linux build + tests with cached CPU libtorch
[X] Docs: README rewrite + docs/{INSTALLATION,GETTING_STARTED,CONFIGURATION,ARCHITECTURE,TROUBLESHOOTING,MIGRATING}.md + rlbot README
[X] Verification: Linux Release build clean; 52/52 tests; CPU training smoke (save/resume), self-play + skill-tracker smoke, 4-min truncation stress (1617 iters, 90% vs old versions, no errors), KL early-stop distribution check, render-mode smoke, 64-game scale run (~27k steps/s on 4-core CPU VM)
